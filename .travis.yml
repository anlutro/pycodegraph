dist: xenial
language: python

python:
  - '3.6'
  - '3.7'
  - '3.8'

install:
  - pip install -e . -r requirements-dev.txt

script:
  - black --check .
  - pylint --errors-only pycodegraph
  - pytest

sudo: false
